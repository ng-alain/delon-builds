import { HttpInterceptorFn } from '@angular/common/http';
/**
 * Cache interceptor
 *
 * 缓存拦截器
 *
 * @example
 * provideHttpClient(withInterceptors([httpCacheInterceptor])),
 */
export declare const httpCacheInterceptor: HttpInterceptorFn;
